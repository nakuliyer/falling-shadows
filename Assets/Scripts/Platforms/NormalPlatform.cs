using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NormalPlatform : Platform
{
    // Start is called before the first frame update
    void Start()
    {
        // Debug.Log("a platform fell");
    }

    // Update is called once per frame
    new void Update()
    {
        base.Update();
    }
}
